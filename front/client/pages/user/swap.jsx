import Navigation from "../../component/Navigation";
import SwapToken from "../../component/Swap";

const Swap = () => {
    return (
        <div>
            
            <SwapToken/>
        </div>
    );
}

export default Swap;